# tetsugaku-app

## プロジェクト概要

哲学者とレスバ（論争）して遊べる Web アプリ。エンターテインメント目的。

**要件定義**: ✅ 完了。
**デザインコンセプト**: ✅ 完了（2025-06-07）。Pencil 再デザイン進行中。
**実装**: 🚧 進行中（4画面 UI + Chrome ローカル LLM 接続完了、デプロイ・肖像画像は未着手）

## 技術スタック

- **フレームワーク**: TanStack Start（Next.js は使わない）
- **デプロイ**: Cloudflare Pages
- **パッケージ管理**: pnpm
- **LLM**: Chrome 内蔵のローカル Gemini（Gemini Nano / Chrome Built-in AI APIs）
  - サーバー側 LLM API は使わない
  - Chrome の `LanguageModel` / Prompt API を使用
- **モノレポ**: Turborepo + pnpm を維持（将来バックエンド追加に備える）
- **移行**: `create-turbo` 生成の `apps/` 配下を削除し、TanStack Start でクリーンスタート

## 設計上の制約

- ローカル LLM のため **Chrome 専用**
- HTTPS または localhost が必要
- ユーザー端末でモデルのダウンロードが必要な場合あり
- MVP はバックエンド不要（会話処理はすべてクライアント側）
- 履歴保存は将来 `apps/backend` + インフラで実装予定

## 決定済み

- アプリの目的: 哲学者とのレスバ遊び
- フレームワーク: TanStack Start
- デプロイ: Cloudflare Pages
- LLM: Chrome ローカル Gemini
- 進行形式: **時間制バトル**（5分 / 8分 / 10分をユーザーが選択、勝敗あり）
- お題: ユーザーが自由入力。思い浮かばないときは **選択式 UI** でお題を選べる
- 哲学者: MVP は固定 4 人（ソクラテス・プラトン・カント・ニーチェ）。将来拡張予定

### ペルソナ方針（暫定）

| 哲学者 | 論じ方の特徴 |
|--------|-------------|
| ソクラテス | 問答法、相手を問い詰める |
| プラトン | イデア論・正義、理想主義 |
| カント | 義務論・理性、冷静に論理展開 |
| ニーチェ | 挑発的、既存価値観を否定 |

### お題の方針

- 抽象論より **私生活・身近な話題** を優先
- 選択式 UI: 5 カテゴリ（人間関係 / 仕事・キャリア / SNS・現代生活 / 自分自身 / 恋愛・性）
- 具体リストは後日決定。データは `packages/common` に JSON 想定

## データ永続化

- MVP: 保存なし（リロードで消える）
- 将来: backend + インフラで履歴保存

## バトル進行（決定済み）

### 制限時間

- ユーザーが **5分 / 8分 / 10分** を選択（お題入力画面で選ぶ）
- チャット画面ヘッダーに残り時間を常時表示

### 終了条件（いずれかで即終了 → リザルト画面へ）

| 条件 | トリガー | 結果 |
|------|---------|------|
| 哲学者の敗北宣言 | 応答に敗北マーカー（`【敗北】`）を含む | ユーザー勝利 |
| ユーザー降参 | 「降参」ボタン（確認ダイアログ付き） | 哲学者勝利 |
| 時間切れ | タイマー 0 | **ユーザー敗北**（時間内に相手の論理を突き崩せなかった） |

優先順位: 降参・敗北宣言 > 時間切れ。対戦中に先に起きた方で終了。

### 哲学者プロンプト変更

- 普段は立場を貫く（現行方針維持）
- 論点が完全に崩れた場合のみ、一度だけ `【敗北】` 付きで敗北宣言可能
- 安易な敗北・譲歩は禁止

## 画面構成（MVP）

1. トップ（キャッチコピー + 3 ステップ説明 + CTA + 注記）
2. 哲学者選択
3. お題入力（自由 + 選択式 + **制限時間 5分/8分/10分 選択**）
4. チャット画面（タイマー + 降参ボタン）
5. **リザルト画面**（勝敗 + 結果コメント + もう一回 / トップへ）

## ブラウザ対応

- Chrome 専用。非対応環境では案内画面のみ
- フォールバック API なし（MVP）

## UI・スタイリング

- Tailwind CSS + shadcn/ui
- MVP: ライトモードのみ。CSS 変数等で将来のダーク / ライト切替に備える

## デザインコンセプト

**キーワード**: ポップパンクな哲学闘技場

真面目な哲学アプリではなく、哲学者とレスバする **エンタメ空間**。闘技場（A）と挑発的パンク（D）を掛け合わせつつ、とっつきやすさを優先する。

### 体験トーン

- 入り口（トップ・哲学者選択）: 闘技場感・ワクワク感
- 実用画面（お題入力・チャット）: 整然・読みやすさ優先
- 案内画面: 哲学者キャラがセリフで案内（Chrome 非対応＝ソクラテス、LLM 未準備＝カント）

### ブランディング

- **アプリ名**: 哲学レスバ
- **UI コピー**: カジュアル・ネット寄り（「レスバ」「誰と〜する？」）
- **ロゴ**: E 案 — 衝突する2吹き出し＋中央 VS バッジのシンボルマーク ＋ 極太ワードマーク
  - 左吹き出し: コーラル朱（`#E85D4C`）
  - 右吹き出し: 白＋黒枠
  - 中央: スタンプ風 VS バッジ
  - 衝突演出: スパーク（短い線）を挟む
  - サイズ: ヘッダー用 `LogoMark`、トップヒーロー用 `LogoMark-Large`

### カラーパレット

| 用途 | 値 |
|------|-----|
| 背景 | `#FAFAF8` |
| テキスト | `#1A1A1A` |
| グローバルアクセント | `#E85D4C`（コーラル朱） |
| ソクラテス | `#2563EB` |
| プラトン | `#7C3AED` |
| カント | `#475569` |
| ニーチェ | `#DC2626` |

### タイポグラフィ

- 見出し・CTA・バッジ: 極太ゴシック（実装時: M PLUS 1p Black 等）
- 本文・チャット: レギュラーゴシック（実装時: Noto Sans JP）
- スタンプ風ラベル: 太字＋軽い傾き＋シール背景（トップ・哲学者選択中心）

### グラフィカル文字（ロゴ・ヒーロー）

**バトルロゴ調**（参考: 特撮ヒーローの「FIGHT!!」ポスター）— 多層アウトライン・スプラッシュ・高コントラスト。

| 要素 | 表現 |
|------|------|
| **ヒーロー** | 「レスバ!!」を巨大化。シアン → オレンジ → 黒 → 白の **4層テキスト** で筆致風の迫力を再現。スプラッシュ楕円・スピードライン・`FIGHT` スタンプ付き |
| **サブコピー** | 「哲学者と、」は黒バナー＋シアン枠のプリファイト風ラベル |
| **ワードマーク** | 「哲学」黒チップ ＋ 「レスバ」多層グロー ＋ 赤い `!!` |
| **カラー** | `$battle-cyan` `#00E5FF` / `$battle-orange` `#FF6B00` / `$battle-red` `#FF1744` / `$battle-yellow` `#FFE100` |
| **背景** | 縦カラーバー（哲学者色）＋オレンジ斜めストライプ — 特撮ポスターの色面分割を参考 |
| **実装時フォント候補** | ヒーロー: **Dela Gothic One** / **RocknRoll One** / **ヒラギノ角ゴ StdN W8** + SVG ロゴ併用。英字 `FIGHT` 部分は **Bangers** / **Impact** 系 |
| **実装時** | 本番は **SVG ロゴ** を使用（`design/assets/` → `apps/web/public/branding/`） |
| **アセット** | `hero-resuba.svg` / `wordmark.svg` / `select-title.svg` / `grunge-noise.svg` |
| **グランジ** | `grunge-noise.svg` を全画面に `opacity: 0.14` でオーバーレイ |

### レイアウト方針

| 画面 | 演出 |
|------|------|
| トップ | 斜めストライプ・非対称ヒーロー・スタンプ（Chrome 専用等） |
| 哲学者選択 | カード軽い傾き（2〜3°）、キャラ色ボーダー、ポップ肖像 |
| お題入力 | 整然。カテゴリはシール風チップ |
| チャット | 整然。アバター付き吹き出し、哲学者色左ボーダー |
| Chrome 非対応 | ソクラテスが問いかけ口調で案内 |
| LLM 未準備 | カントが理性的に案内 |

### 哲学者ビジュアル

- **画風**: ポップイラスト（太線・フラット塗り）で統一
- **キャラネタ**: ソクラテス＝問い詰めポーズ、プラトン＝洞窟/本モチーフ、カント＝眼鏡・冷静、ニーチェ＝挑発的笑み/サングラス
- **肖像**: AI 生成 + 選定 → `apps/web/public/philosophers/`

## モノレポ構成

```
apps/
  web/          # TanStack Start（フロントエンド）— MVP の主戦場
  backend/      # バックエンド（将来）
packages/
  common/       # 共有ロジック・型定義
  ui/           # 共有 UI コンポーネント（shadcn/ui ベース）
  tsconfig/     # 共有 TypeScript 設定
```

## MVP スコープ

- 実装対象: `apps/web` + `packages/{common,ui,tsconfig}`
- `apps/backend` は MVP では作らない

## 言語

- 日本語のみ。哲学者も現代日本語で論じる（ペルソナの思想・論調は維持）

## LLM 応答表示

- API はストリーミングで生成。UI はローディング表示 → 完了後に一括表示

## リロード・離脱時の UX

- チャット画面に「会話は保存されません」を常時表示
- `beforeunload` 等で離脱確認

## Chrome ローカル LLM 未準備時

- 起動時に `LanguageModel` API の可用性をチェック
- 不可なら手順付き案内画面を表示

## 哲学者選択 UI

- イラスト / 肖像画像 + 名前・キャッチコピー・説明のカード形式

## 肖像画像

- AI 生成 + 選定。画風統一。`apps/web/public/philosophers/` に配置想定

## 将来のバックエンド（履歴保存時）

- Hono on Cloudflare Workers（決定）
- DB は履歴機能実装時に決定（第一候補: Turso。D1 / Supabase も比較）

## 未決定（実装前に決める）

- 選択式お題の具体リスト
- 将来の DB 最終選定

## デザイン・実装の進め方

1. **Pencil** で画面デザインを作り込む（コンセプト確定済み、再デザイン進行中）
2. デザイン確定後: クリーンスタート（`apps/` スターター削除 → TanStack Start 新設）
3. Pencil デザインに沿って UI 実装 → LLM 接続 → デプロイ

**重要**: デザイン確定前に UI を勝手に実装しない。

## 実装時の優先順位（デザイン確定後）

1. クリーンスタート
2. Pencil デザインに基づく 4 画面 UI
3. Chrome ローカル LLM 接続 + ペルソナプロンプト
4. お題データ（`packages/common`）+ 肖像画像
5. Cloudflare Pages デプロイ

## 開発コマンド

```sh
pnpm install   # 依存関係インストール
pnpm dev       # 開発サーバー起動
pnpm build     # ビルド
pnpm lint      # リント
```
